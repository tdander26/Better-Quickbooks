// Temporary placeholder — replaced by the real Dashboard in the feature build.
import { PageHeader, Card } from "@/components/ui";

export default function Home() {
  return (
    <div>
      <PageHeader title="Dashboard" subtitle="Coming together…" />
      <Card className="p-6">
        <p className="muted text-sm">Setting things up.</p>
      </Card>
    </div>
  );
}
